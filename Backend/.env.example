# Copy this file to .env and fill values
# PostgreSQL example:
# DATABASE_URL=postgresql+psycopg2://postgres:yourpassword@localhost:5432/flightly
# Or SQLite absolute path on PythonAnywhere:
# DATABASE_URL=sqlite:////home/<youruser>/Flight-ticketing/Backend/dev.db

# JWT secret (change in production)
JWT_SECRET=changeme
# Token TTL minutes
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Deployed frontend origin for CORS (e.g., https://your-frontend.netlify.app)
# DEPLOY_ORIGIN=
