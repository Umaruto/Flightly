JWT_SECRET=changeme
# Optional: if not set, SQLite dev.db is used
# DATABASE_URL=postgresql://user:pass@localhost:5432/flight

# JSON array of allowed origins
CORS_ALLOW_ORIGINS=["http://localhost:5173"]

# Optional: allow dynamic origins via regex, e.g., all Vercel preview URLs
# CORS_ALLOW_ORIGIN_REGEX=https://.*-yourapp.vercel.app
# Copy this file to .env and fill values
# PostgreSQL example:
# DATABASE_URL=postgresql+psycopg2://postgres:yourpassword@localhost:5432/flightly

# JWT secret (change in production)
JWT_SECRET=changeme
# Token TTL minutes
ACCESS_TOKEN_EXPIRE_MINUTES=60
